# DB plus Markdown knowledge base

The platform will store workflow state, indexes, versions, and scheduling metadata in the database, while emitting human-readable research artifacts into a Markdown knowledge base with wikilinks. This preserves machine-manageable state without giving up durable, inspectable research documents or future RAG-readiness.
